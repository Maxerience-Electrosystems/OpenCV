#include<stdio.h>

      int main()
     {
      int i,j;
	for(i=1;i<=5;i++)
	{	for(j=1;j<=10;j++)
	{
			//printf("Mat s[%d];\n",i,j);
            printf("fs[\"s%d_%d\"] >> s[%d];\n",i,j,(i-1)*10+j-1) ;
            printf("std::cout << s[%d]<<endl;\n\n",(i-1)*10+j-1);
	}
   } return 0;
  }
